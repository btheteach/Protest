import { SafeAreaView, View, Text, ScrollView} from 'react-native'

const HomeScreen = () => {
    return(
        <ScrollView>
            <Text>Home</Text>
        </ScrollView>
    )
}

export default HomeScreen
